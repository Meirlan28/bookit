# Leave empty when frontend and backend are served through the same origin/proxy.
# For a separate API origin, include the full base URL, for example:
# VITE_API_URL=http://localhost:8000
VITE_API_URL=
